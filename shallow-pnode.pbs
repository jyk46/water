#!/bin/sh -l

#PBS -l nodes=1:ppn=24
#PBS -l walltime=0:30:00
#PBS -N shallow-pnode
#PBS -j oe

# if not called with SIZE=???, set to 200
DISPATCH_SIZE=${SIZE:-200}

module load cs5220
cd $PBS_O_WORKDIR
./shallow-pnode -x 2 -y 2 -b 1 -i dam_break -o dam_break.out -n $DISPATCH_SIZE
